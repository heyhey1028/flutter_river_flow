name: flutter_river_flow
description: A new Flutter project.

# publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  freezed_annotation:
  riverpod_annotation: ^2.1.1
  firebase_core:
  flutter_riverpod: ^2.3.6
  firebase_auth:
  http: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner:
  freezed:
  riverpod_generator: ^2.2.3

flutter:
  uses-material-design: true

  assets:
    - assets/images/
